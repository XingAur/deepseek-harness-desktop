{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "runtime-provisioning-local",
  "description": "Minimal core permissions for the local Runtime provisioning window.",
  "windows": ["provisioning"],
  "permissions": [
    "core:default"
  ]
}
